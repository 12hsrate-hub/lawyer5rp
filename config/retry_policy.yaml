retry_policy:
  max_retries: 2
  retry_backoff_seconds: 0

non_retryable_failures: []

retryable_failures:
  - validator_blocker
  - transient_model_error
  - timeout
  - upstream_5xx

fallbacks:
  validator_blocker: factual_fallback_expanded
