# Compatibility Seam Note

- Seam ID: `seam-2026-04-law-source-set-runtime-promotion`
- Area: `law-domain canonical source-of-truth transition`
- Reason this seam exists: the current runtime still bridges between `server_config.law_qa_sources`, optional workflow-backed `law_sources_manifest`, runtime `law_sets`, and imported `law_versions`, while the target model should be `SourceSet -> discovered links -> canonical law documents -> explicit runtime promotion`.
- Current source of truth: server-effective law context is still derived from flat source URLs plus runtime rebuild/import behavior, with `law_sets` and `law_versions` acting as both runtime artifacts and partial ingestion truth.
- Target source of truth: canonical upstream law state should come from workflow-backed reusable `SourceSet` revisions, discovery runs, canonical `LawDocument` identities with URL aliases, and explicit server-to-source-set bindings, while runtime `law_sets`/`law_versions` become projections only.
- What changed in this task: added the first persistence foundation for reusable `source_sets`, immutable `source_set_revisions`, and `server_source_set_bindings`, plus read-only admin surfaces that expose source sets, revisions, and server bindings, an explicit compatibility backfill action that imports current legacy flat law-source URLs into `legacy_flat` source-set revisions, the first persisted discovery layer with `source_discovery_runs` and `discovered_law_links`, including a deterministic manual discovery execution path for `legacy_flat` and passthrough-compatible revisions plus admin visibility for discovery runs and extracted law links, an additive canonical law-document foundation (`canonical_law_documents` plus aliases) with bounded ingest/read surfaces over discovery runs, a first additive canonical law-document-version seed stage that records seeded fetch/parse provenance from discovery links, a bounded manual fetch stage that can translate seeded canonical document versions into `fetched` or `failed` content-bearing versions, a bounded manual parse stage that can translate fetched canonical document versions into `parsed` or `failed` structured text-bearing versions, the first persisted server-effective law projection preview layer (`server_effective_law_projection_runs` plus preview items) that can materialize deterministic preview candidates from active bindings and parsed canonical document versions, an explicit `approved`-only materialization bridge that can turn a projection preview into an unpublished legacy `law_set` draft, and a narrower activation bridge that still publishes the materialized legacy `law_set` for rollback-safe compatibility but now imports the active runtime snapshot directly from the selected parsed canonical projection items instead of re-running the legacy flat-source rebuild over source URLs. That keeps `persist_sources` side effects out of the bridge, preserves the existing runtime `law_version`/rollback shell, and makes projection-derived runtime chunks the primary activation payload rather than a legacy source-list reconstruction. An aggregated operator read path can show projection-run provenance, materialized `law_set` detail, activation provenance, and current active runtime `law_version` alignment in one payload, and runtime server health, item/list, and law-sources status payloads can now expose a compact projection-bridge summary and mark projection provenance as the primary explanation when canonical projection provenance exists for that server.
- Additional narrowing in the current slice: admin runtime health and server laws workspace now expose an explicit `runtime_provenance` summary that classifies the current runtime shell as `projection_backed`, `projection_drift`, `legacy_runtime_shell`, `materialized_shell_only`, or `uninitialized`. This does not change activation behavior, but it makes the remaining seam visible without treating legacy `law_set` / `law_version` rows as the only explanation source in admin read models.
- Additional narrowing in the latest slice: server-centric workspace readiness and observability now treat non-`projection_backed` runtime provenance as an explicit operator-visible compatibility signal. `legacy_runtime_shell`, `projection_drift`, and `materialized_shell_only` now surface as dedicated warnings/issues with safe `recheck` handoff, and workspace readiness marks them as `stale_changes` instead of silently presenting the runtime shell as fully ordinary.
- Additional narrowing in the current slice: admin runtime health, server laws summary, and laws diff payloads now expose explicit `runtime_alignment` details with active/projected `law_set` and `law_version` identifiers plus an `aligned / drift / legacy_only / pending_activation / uninitialized` status. This still leaves the legacy runtime shell in place, but it makes parity with the promoted projection visible in one operator-facing read model instead of forcing manual comparison across screens.
- Additional narrowing in the latest slice: server laws summary and diff now expose `runtime_item_parity`, a compact identity-level comparison between the active runtime `law_set` items and the latest projection items. Operators can now see whether runtime and projection are aligned by law identity, plus counts for `runtime_only`, `projection_only`, and `shared` items, without changing the legacy publish/rollback bridge.
- Additional narrowing in the current slice: item-level drift is now promoted into server overview/issues as an explicit operator signal. When runtime `law_set` items and latest projection items differ by law identity, admin surfaces emit a dedicated `laws_runtime_item_parity` warning with safe `recheck` handoff, so composition drift no longer hides behind laws-tab-only summaries.
- Additional narrowing in the latest slice: the same item-level drift read model now carries compact sample keys and a human-readable `drift_summary` across server workspace, laws summary, laws diff, and issues. Operators can now see representative `runtime_only` and `projection_only` law identities directly from overview/issues, without leaving the high-level runtime triage path.
- Additional narrowing in the current slice: admin runtime surfaces now expose a separate `runtime_version_parity` summary built from active runtime and promoted projection version identifiers. This adds a read-only version/provenance parity layer to workspace/laws/issues, including a dedicated `laws_runtime_version_parity` warning with safe `recheck`, while keeping the legacy publish/rollback bridge untouched.
- Additional narrowing in the latest slice: admin runtime surfaces now also expose a compact `projection_bridge_lifecycle` summary with `preview_only / materialized / activated / drifted / uninitialized` states. This makes the bridge stage itself visible in workspace/laws/issues and adds a dedicated `laws_projection_bridge_lifecycle` warning with safe `recheck`, without changing materialization, activation, or rollback behavior.
- Additional narrowing in the current slice: admin runtime surfaces now expose a compact `projection_bridge_readiness` matrix with blockers, next-step guidance, and basic counters for bindings, selected laws, content readiness, and bridge stage. This promotes bridge triage into a single operator-facing summary and adds a dedicated `laws_projection_bridge_readiness` warning with safe `recheck`, while leaving the actual publish/rollback bridge unchanged.
- Additional narrowing in the latest slice: admin runtime surfaces now expose a compact `promotion_candidate` summary that combines bridge readiness, latest diff, content readiness, and runtime-version parity into one operator-facing “can this move forward?” read model. This adds a dedicated `laws_promotion_candidate` warning with safe `recheck`, while still leaving actual promotion, activation, and rollback behavior unchanged.
- Additional narrowing in the current slice: admin runtime surfaces now expose a compact `promotion_delta` summary that turns latest diff and content gaps into a single operator-facing review signal. This adds a dedicated `laws_promotion_delta` warning with safe `recheck`, so operators can see whether a promoted candidate is materially changing or still blocked by content gaps without opening the full diff first.
- Additional narrowing in the latest slice: admin runtime surfaces now also expose a compact `promotion_blockers` summary that deduplicates readiness blockers, bridge stage mismatches, runtime parity drift, and promotion-review signals into one short operator-facing list. This adds a dedicated `laws_promotion_blockers` warning with safe `recheck`, so overview/issues can answer “what still blocks a clean promoted runtime state?” without forcing the operator to read multiple separate summaries first.
- Additional narrowing in the current slice: admin runtime surfaces now also expose a compact `activation_gap` summary that isolates the specific distance between the latest promoted projection and the active runtime shell. This adds a dedicated `laws_activation_gap` warning with safe `recheck`, so operators can distinguish “candidate/problem blockers” from the narrower question of whether runtime is simply still waiting for the activation step.
- Additional narrowing in the latest slice: admin runtime surfaces now also expose a compact `runtime_shell_debt` summary that estimates how strongly a server still depends on the legacy runtime shell path. This adds a dedicated `laws_runtime_shell_debt` warning with safe `recheck`, so operators can distinguish activation/readiness issues from the broader compatibility debt of still relying on legacy `law_sets / law_versions` as a runtime shell.
- Additional narrowing in the current slice: admin runtime surfaces now also expose a compact `runtime_convergence` summary that combines promotion blockers, activation gap, and runtime-shell debt into one short operator-facing convergence status. This adds a dedicated `laws_runtime_convergence` warning with safe `recheck`, so overview/issues can answer “how close is this server to fully projection-backed runtime?” without reading several separate summaries.
- Additional narrowing in the latest slice: admin runtime surfaces now also expose a compact `cutover_readiness` summary that estimates whether a server is actually ready for a controlled shrinking of the compatibility bridge. This adds a dedicated `laws_cutover_readiness` warning with safe `recheck`, so operators can distinguish “runtime observability is good” from the stricter question of whether the server is ready for a deliberate cutover discussion.
- Additional narrowing in the current slice: admin runtime surfaces now also expose a practical `bridge_shrink_checklist` and `cutover_blockers_breakdown`. Together they turn cutover planning into an operator-facing checklist plus blocker categories, with dedicated `laws_bridge_shrink_checklist` and `laws_cutover_blockers_breakdown` warnings and safe `recheck`, so admins can see not just whether cutover is close, but which specific preparation steps and blocker groups still hold the server in compatibility mode.
- Additional narrowing in the latest slice: advisory-only promotion review signals (`review_needed`, `delta_attention`) no longer block `runtime_convergence`, `cutover_readiness`, or `cutover_blockers_breakdown` when runtime is already projection-backed, activated, version-aligned, activation-closed, and low-debt. They still remain visible as operator review prompts, but they no longer falsely present an otherwise converged server as cutover-blocked.
- Additional narrowing in the current slice: advisory promotion-review state is now split out into a dedicated `promotion_review_signal` read model instead of leaking through `promotion_blockers` as a warn-only pseudo-blocker. Admin workspace/laws/issues can still show “review this diff” guidance, but hard blocker triage now stays reserved for states that actually prevent clean runtime convergence or cutover readiness.
- Additional narrowing in the latest slice: admin runtime surfaces now also expose an explicit `runtime_cutover_mode` summary that classifies servers as `compatibility_mode`, `cutover_candidate`, or `projection_preferred`. This still does not change activation/rollback behavior, but it turns the remaining bridge state into a policy-facing signal that is much closer to a real controlled shrinking path than the earlier read-only convergence diagnostics.
- Additional narrowing in the current slice: admin runtime surfaces now also expose a single `runtime_bridge_policy` verdict that combines resolution policy and cutover mode into operator-facing states like `keep_compatibility`, `stabilize_for_cutover`, or `prefer_projection_runtime`. This still leaves the compatibility bridge intact, but it compresses the remaining bridge posture into one policy decision surface instead of forcing operators to reconstruct it from several lower-level summaries.
- Additional narrowing in the latest slice: runtime multi-server guardrails are now explicit. Admin/runtime surfaces expose `runtime_operating_mode`, `runtime_policy_violations`, and `cutover_guardrails`, so servers that are supposed to move toward projection-preferred runtime now surface silent fallback/bootstrap/legacy-debt regressions as explicit policy problems instead of burying them inside lower-level diagnostics.
- Additional narrowing in the current slice: admin/runtime surfaces now also expose `runtime_policy_enforcement`, which distinguishes enforced projection policy, compatibility hold, pre-enforcement transition, and outright policy violation. This is still read-model only, but it moves the remaining compatibility bridge from passive diagnostics into a clearer staged enforcement model.
- Additional narrowing in the latest slice: admin/runtime surfaces now also expose `policy_breach_summary` and `runtime_risk_register`. This turns the remaining compatibility bridge debt into an explicit risk surface with breach semantics and open risk items, rather than leaving risk interpretation scattered across several intermediate policy summaries.
- Additional narrowing in the current slice: admin/runtime surfaces now also expose `runtime_governance_contract`, `legacy_path_allowance`, `compatibility_exit_scorecard`, and `runtime_breach_categories`. This still leaves the legacy bridge behavior unchanged, but it upgrades the seam from generic “risk visibility” to an explicit per-server governance contract: operators can now see which legacy paths are still allowed, whether the server is genuinely close to exiting compatibility, and which breach categories still keep the compatibility shell alive.
- Additional narrowing in the latest slice: admin/runtime surfaces now also expose `legacy_path_controls`, `projection_runtime_gate`, `compatibility_shrink_decision`, and `runtime_exception_register`. This still does not change activation/rollback behavior, but it moves the seam one step closer to real shrinking work: the server now shows which legacy paths are blocked vs transition-only vs still allowed, whether the projection-runtime gate is open, whether compatibility should still be held, and which explicit exceptions are still being carried.
- Additional narrowing in the current slice: admin/runtime surfaces now also expose `compatibility_path_matrix`, `next_shrink_step`, and `shrink_sequence`. This still does not change activation/rollback behavior, but it upgrades the seam from generic shrinking posture to bounded execution planning: operators can now see the per-path state for `neutral_fallback`, `bootstrap_pack`, and `legacy_runtime_shell`, the next recommended shrink target, and the ordered sequence for reducing the remaining compatibility shell.
- Additional narrowing in the latest slice: admin runtime onboarding/workflow readiness no longer treats `active_law_set` as hidden configuration truth. Explicit law source configuration plus bindings now define the workflow-ready path, while the active legacy `law_set` remains visible only in health/provenance/runtime-shell summaries. This shrinks the role of `law_sets / law_versions` toward shell/rollback artifacts without changing activation or rollback behavior.
- Why this was necessary: the repo needed a formal architecture and migration contract for replacing flat per-server law-source handling with a source-set-driven ingestion and promotion model, without silently rewriting the runtime path.
- Rollback path: continue using the existing flat-source compatibility model (`server_config`, `law_sources_manifest`, runtime rebuild/import) until source sets, bindings, discovery, canonical document ingest, and runtime promotion all reach parity for the target servers.
- Removal gate: remove or shrink this seam only after active servers resolve law runtime state from source-set bindings and promoted projections by default, legacy flat source imports are purely migration-only, and admin/runtime surfaces can explain provenance, duplicates, disappearance states, and rollback without referencing flat-source truth.
- Tests covering this seam:
  - future source-set migration parity tests must compare legacy flat-source rebuild output against the new projection path
  - future admin/runtime contract tests must prove provenance, duplicate resolution, disappearance handling, and activation/rollback remain explainable
- Remaining risks:
  - canonical law identity exists only in additive `url_seed` form; rich parsed identity and remap policies do not exist yet in code
  - discovery execution is still manual and passthrough-oriented; no canonical discovery job or rich adapter layer exists yet
  - canonical law-document versions now support manual bounded fetch and parse, projection preview runs can be approved/held, approved projection runs can be materialized into unpublished legacy `law_set` drafts, and those drafts can be activated through the narrow legacy publish/rebuild bridge, but there is still no rich fetch scheduler, no advanced parser pipeline, no fully canonical runtime read path, and no removal of the legacy flat-source compatibility model
  - runtime still depends on flat source URLs and rebuild-side effects
  - disappearance/quarantine/promotion semantics are not yet implemented
  - migration complexity is high because existing servers already rely on legacy flat source flows
